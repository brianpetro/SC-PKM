# AG3 computer-use prompt - Pro Trial / Large Vault Upgrade

Capture only real Connections Pro product depth.

Profile:

```text
sc-pro-clean
```

Required fixture:

```text
smart-connections-demo-v2 / 2.0.0
```

Do not use generic hero art, Smart Loop imagery, Smart Dedupe, settings screens, progress modals, errors, or command lists as primary raw candidates.

Verify and then capture these evidence families:

1. `sc-inline-block-related-results` / inline popover
2. `sc-inline-block-related-results` / full list opened for the block
3. `sc-bases-reference-relevance-score` / fixed reference and descending score
4. `sc-bases-related-list-column` / real `list_connections` link trails

For each family, capture three separately composed raw variants:

- landscape for `1200 x 628`
- square for `1200 x 1200`
- portrait for `960 x 1200`

Use `02_CAPTURE_MATRIX.csv` for exact raw and final filenames.

Critical pass conditions:

- target paragraph and inline result relationship are visible
- full list remains visibly anchored to the target block or line range
- Base score column is populated and really sorted descending
- related-link column uses real documented formula output
- no static image claims measured speed, a universal score, or a transition it does not show
- every raw capture is untouched and fully traceable

Stop rather than fabricate a cleaner result.
