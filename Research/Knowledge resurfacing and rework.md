# Knowledge resurfacing and rework

The value is not faster searching. It is avoiding the work of reconstructing context that was already captured somewhere else.

When a related note returns at the moment of writing, the user can reuse judgment instead of rebuilding it from memory.

